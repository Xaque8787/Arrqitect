schema_version: 2

app:
  id: prowlarr
  name: Prowlarr
  version: "1.0.0"
  flavor: linuxserver

description: Indexer manager and proxy for Radarr, Sonarr and other *arr apps.
icon_url: https://raw.githubusercontent.com/Prowlarr/Prowlarr/develop/Logo/256.png

services:
  - id: prowlarr
    image:
      repository: lscr.io/linuxserver/prowlarr
      tag: latest

    networking:
      ports:
        - id: web_ui
          listen_port: 9696
          protocol: http
          reachability: external

    storage:
      - id: config
        persistence: persistent
        sharing: private
        mutability: read-write
        durability: configuration
        container_path: /config

    lifecycle:
      restart:
        behavior: persistent

provides:
  - key: prowlarr.url_internal
    type: endpoint
    sensitive: false
    rotates: false

  - key: prowlarr.api_key
    type: credential
    sensitive: true
    rotates: true

consumes: []

config_schema:
  - id: web_ui_port
    label: Web UI Port
    type: port
    default: 9696
    binds_to: services.prowlarr.networking.ports.web_ui.published_port
    required: true

  - id: config_path
    label: Config Path
    type: storage_path
    default: ./config
    binds_to: services.prowlarr.storage.config.host_path
    required: true

hooks:
  post_install: hooks/post_install.yaml
  pre_remove: hooks/pre_remove.yaml
